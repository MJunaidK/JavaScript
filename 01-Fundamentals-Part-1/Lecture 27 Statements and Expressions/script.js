// Expressions: an expression is a piece of code that produces a value
3 + 4;
1991
true && false && !false

// Statement: statement is like a bigger piece of code that is executed and which does not produce a value on itself 
if(23 > 10){
  const str = '23 is bigger';
}

const me  = 'Junaid';
console.log(`I'm ${2037 -1984} years old ${me}`);