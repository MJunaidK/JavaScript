const firstName = "Junaid";
const job = 'Architect';
const birthYear = 1984;
const year = 2022;

const junaid = "I'm " + firstName + ', a ' +  (year - birthYear) + ' years old ' + job + '!';
console.log(junaid);

const junaidNew = `I'm ${firstName}, a ${year-birthYear} years old ${job}!`;
console.log(junaidNew);

console.log(`Just a regular string...`)

console.log('String with \n\
multiple \n\
lines \n\
using single quotes');


console.log(`String with 
multiple 
lines 
using back ticks`);

